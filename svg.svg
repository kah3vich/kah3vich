<svg sandbox="allow-same-origin allow-scripts allow-popups allow-forms allow-same-origin" xmlns="http://www.w3.org/2000/svg" width="500" height="500">
  <rect id="myRect" x="50" y="50" width="100" height="100" fill="red" />
  <script>
    // Get the URL parameters
    const urlParams = new URLSearchParams(window.location.search);
    
    // Get the value of the "color" parameter
    const color = urlParams.get('color');
    
    // Set the fill color of the rectangle based on the "color" parameter
    console.log(color)
    if (color) {
      document.getElementById('myRect').setAttribute('fill', color);
    }
  </script>
</svg>