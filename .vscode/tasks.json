{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clear packages",
            "type": "shell",
            "command": "rm -rf node_modules; rm -rf package-lock.json; rm -rf yarn.lock;"
        }
    ]
}